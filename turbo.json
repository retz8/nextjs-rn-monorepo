{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "**/*.md"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": ["node_modules/**"]
    },
    "lint": {},
    "clean": {
      "cache": false
    },
    "install": {
      "cache": false,
      "dependsOn": ["^install"]
    }
  }
}
